#ifndef SETTINGS_H
#define SETTINGS_H

const double ALPHA = 0.1;                   // learning rate multiplier
const size_t HIDDEN_LAYERS = 1;             // number of hidden layers
const size_t HIDDEN_LAYER_NEURONS = 1;     // number of neurons in a hidden layer
const size_t INPUT_SIZE = 1;               // size of the input layer
const size_t OUTPUT_SIZE = 1;              // size of the output layer
const size_t DATASET_SIZE = 60000;          // Number of data samples to train on
const size_t TEST_DATA_SIZE = 10000;

#endif // SETTINGS_H
